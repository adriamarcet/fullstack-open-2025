{
  "name": "persons-backend",
  "version": "0.0.0",
  "description": "Backend for Phonebook app",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf dist && cd ../phonebook && npm run build && cp -r dist ../persons-backend",
    "build:full": "npm run build:ui && rsync -av --delete --exclude 'node_modules' --exclude '.git' . ../../../01.fullstack-open-render-test/ && cd ../../../01.fullstack-open-render-test && git add . && git commit -m 'Update backend' && git push origin main",
    "dev": "node --watch index.js",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Adrià Marcet",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mongodb": "^7.0.0",
    "mongoose": "^8.19.3",
    "morgan": "^1.10.1"
  }
}
